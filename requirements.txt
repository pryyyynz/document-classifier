# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# PDF extraction and processing
pdfplumber>=0.11.4
PyMuPDF>=1.24.9
python-docx>=0.8.11

# OCR (for scanned PDFs)
opencv-python>=4.10.0.84
pytesseract>=0.3.13
Pillow>=9.0.0

# Machine Learning
scikit-learn>=1.5.2

# Explainability
lime>=0.2.0.1

# Transformer models (optional - for deep learning pipeline)
torch>=2.6.0
transformers>=4.44.2

# System monitoring (for transformer training)
psutil>=5.9.0

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Testing
pytest>=7.0.0

# Legacy scripts only (not needed for main pipeline)
# reportlab>=4.0.7  # Only used in preprocessing/legacy/
# requests>=2.31.0  # Only used in preprocessing/legacy/
# spacy>=3.7.6      # Optional, not used in main pipeline

